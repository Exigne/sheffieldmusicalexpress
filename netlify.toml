[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# REMOVE the extensions block completely
# REMOVE the catch-all redirect to index.html (Next.js handles routing)
