[build]
  command = "npm run build"
  publish = ".next" # NOT 'dist'

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# REMOVE all redirects from here for now. 
# The Next.js plugin handles them automatically.
